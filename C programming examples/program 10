// Multiplication table.
// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,i,f;
    printf("enter the number : ");
    scanf("%d",&n);
    for(i=1;i<=10;i++){
        f=n*i;
        printf("%d * %d = %d\n",n,i,f);
        
    }
    

    return 0;
}
